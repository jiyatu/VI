# [Value Iteration with Metcalfe's law]in TensorFlow

The realization of Metcalfe-Drop module to avoid overfitting in low-resource image classification".


## Dependencies
* Python >= 3.6
* TensorFlow >= 1.2

